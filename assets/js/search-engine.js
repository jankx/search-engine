(() => {
    var S = (g, e) => () => (e || g((e = { exports: {} }).exports, e), e.exports); var $ = S(() => {
        var f = class {
            state = { q: "", filters: {}, sort: "relevance", page: 1 }; settings = {}; debounceTimer = null; $results = null; $pagination = null; $selectedFilters = null; $featuredItems = null; constructor() { this.$results = document.querySelector(".jankx-search-results-container .results-grid"), this.$pagination = document.querySelector(".pagination-container"), this.$selectedFilters = document.querySelector(".jankx-search-selected-filters"), this.$featuredItems = document.querySelector(".jankx-featured-items"), this.init() } init() { this.collectSettings(), this.collectInitialState(), this.bindEvents() } collectInitialState() { let e = document.querySelector(".jankx-search-sorter .sort-select"); e && (this.state.sort = e.value); let s = document.querySelector(".jankx-search-keyword .search-input"); s && (this.state.q = s.value); let t = new URLSearchParams(window.location.search);["featured_item_category", "industry", "category", "thought_leader"].forEach(r => { if (t.has(r)) { this.state.filters[r] || (this.state.filters[r] = []); let a = t.get(r); this.state.filters[r].includes(a) || this.state.filters[r].push(a); let c = document.querySelector(`.jankx-search-filters input[name="filter[${r}][]"][value="${a}"]`); c && (c.checked = !0) } }) } collectSettings() { document.querySelectorAll("[data-settings]").forEach(s => { let i = s.getAttribute("data-settings"); if (i) try { let t = JSON.parse(i); this.settings = { ...this.settings, ...t } } catch (t) { console.error("Failed to parse settings", t) } }) } bindEvents() { document.addEventListener("input", e => { let s = e.target; if (s.closest(".jankx-search-keyword .search-input")) { let i = s.value; this.state.q = i, this.state.page = 1, this.updateSelectedFilterUI("search_keyword", "current_query", i, !!i, ""), this.updateFeaturedItemsVisibility(), this.debounceSearch() } }), document.addEventListener("change", e => { let s = e.target; if (s.matches('.jankx-search-filters input[type="checkbox"]')) { let i = s.name.match(/filter\[([^\]]+)\]/); if (i) { let t = i[1]; this.state.filters[t] || (this.state.filters[t] = []); let r = s.value, a = s.closest("label"), c = a ? a.querySelector(".term-name") : null, o = c ? (c.textContent || "").trim() : r, u = this.decodeHTMLEntities(o), l = s.closest(".filter-group"), n = l ? l.querySelector(".filter-title") : null, d = n ? (n.textContent || "").trim() : ""; s.checked ? (this.state.filters[t].push(r), this.updateSelectedFilterUI(t, r, u, !0, d)) : (this.state.filters[t] = this.state.filters[t].filter(h => h !== r), this.updateSelectedFilterUI(t, r, u, !1, d)), this.updateFeaturedItemsVisibility(), this.state.page = 1, this.search() } } }), document.addEventListener("change", e => { let s = e.target; s.closest(".jankx-search-sorter .sort-select") && (this.state.sort = s.value, this.search()) }), document.addEventListener("click", e => { let i = e.target.closest(".pagination-container .page-number"); if (i) { e.preventDefault(); let t = i.getAttribute("data-page"); t && (this.state.page = parseInt(t, 10), this.search(), this.$results && window.scrollTo({ top: this.$results.getBoundingClientRect().top + window.scrollY - 100, behavior: "smooth" })) } }), document.addEventListener("click", e => { let i = e.target.closest(".selected-filter-item .remove-filter"); if (i) { e.preventDefault(); let t = i.closest(".selected-filter-item"); if (t) { let r = t.getAttribute("data-taxonomy"), a = t.getAttribute("data-term-id"); if (r === "search_keyword") { this.state.q = ""; let c = document.querySelector(".jankx-search-keyword .search-input"); c && (c.value = ""), this.updateSelectedFilterUI(r, a || "", "", !1), this.updateFeaturedItemsVisibility(), this.state.page = 1, this.search() } else if (r && a && this.state.filters[r]) { this.state.filters[r] = this.state.filters[r].filter(o => o !== a); let c = document.querySelector(`.jankx-search-filters input[name="filter[${r}][]"][value="${a}"]`); c && (c.checked = !1), this.updateSelectedFilterUI(r, a, "", !1), this.updateFeaturedItemsVisibility(), this.state.page = 1, this.search() } } } }), document.addEventListener("click", e => { let s = e.target; if (s.matches(".clear-all-filters-btn") || s.closest(".clear-all-filters-btn")) { e.preventDefault(), this.state.filters = {}, document.querySelectorAll('.jankx-search-filters input[type="checkbox"]').forEach(t => { let r = t; r.checked = !1 }), this.state.q = ""; let i = document.querySelector(".jankx-search-keyword .search-input"); i && (i.value = ""), this.clearAllSelectedFiltersUI(), this.updateFeaturedItemsVisibility(), this.state.page = 1, this.search() } }) } debounceSearch() { this.debounceTimer && window.clearTimeout(this.debounceTimer), this.debounceTimer = window.setTimeout(() => { this.search() }, 500) } async search() { if (!this.$results) return; this.showSkeleton(); let e = new FormData; e.append("action", "jankx_search_query"), e.append("nonce", jankx_search_config.nonce); let s = []; Object.keys(this.settings).forEach(t => { t.startsWith("post_type_") && (this.settings[t] === "true" || this.settings[t] === !0) && s.push(t.replace("post_type_", "")) }); let i = { ...this.settings, ...this.state, post_types: s }; e.append("state", JSON.stringify(i)); try { let r = await (await fetch(jankx_search_config.ajax_url, { method: "POST", body: e })).json(); r.success && (this.$results.innerHTML = r.data.html, this.$pagination && (this.$pagination.innerHTML = r.data.pagination), this.$pagination && (this.$pagination.innerHTML = r.data.pagination), r.data.term_counts !== void 0 && this.updateTermCounts(r.data.term_counts), r.data.selected_filters !== void 0 && this.syncSelectedFiltersFromHTML(r.data.selected_filters)) } catch (t) { console.error("Search failed:", t), this.$results.innerHTML = '<p class="error-message">An error occurred while searching. Please try again.</p>' } finally { this.$results.classList.remove("loading"), this.$results.style.opacity = "1" } } updateTermCounts(e) { document.querySelectorAll('.jankx-search-filters input[type="checkbox"]').forEach(i => { let t = i, r = t.value, a = e[r] ?? 0, c = t.closest("label"); if (c) { let o = c.querySelector(".term-count"); o && (o.textContent = `(${a})`), a === 0 && !t.checked ? (c.classList.add("disabled"), t.disabled = !0) : (c.classList.remove("disabled"), t.disabled = !1) } }) } showSkeleton() { this.$results && (this.$results.classList.add("loading"), this.$results.innerHTML = this.getSkeletonHtml()) } getSkeletonHtml() {
                let e = ""; for (let i = 0; i < 3; i++)e += `
                <div class="jankx-skeleton-item">
                    <div class="skeleton-img"></div>
                    <div class="skeleton-text">
                        <div class="skeleton-line title"></div>
                        <div class="skeleton-line"></div>
                        <div class="skeleton-line"></div>
                        <div class="skeleton-line" style="width: 40%"></div>
                    </div>
                </div>
            `; return e
            } clearAllSelectedFiltersUI() { if (!this.$selectedFilters) return; let e = this.$selectedFilters.querySelector(".selected-filters-list"); e && (e.innerHTML = ""), this.$selectedFilters.style.display = "none" } updateSelectedFilterUI(e, s, i, t, r = "") { if (!this.$selectedFilters) return; i = this.decodeHTMLEntities(i); let a = this.$selectedFilters.querySelector(".selected-filters-list"); if (t) { if (!a) { let n = document.createElement("ul"); n.className = "selected-filters-list"; let d = this.$selectedFilters.querySelector(".clear-all-wrapper"); d ? this.$selectedFilters.insertBefore(n, d) : this.$selectedFilters.prepend(n), a = n } if (e !== "search_keyword") { let n = document.querySelector(".jankx-search-keyword .search-input"); n && n.value && !a.querySelector('.selected-filter-item[data-taxonomy="search_keyword"]') && this.updateSelectedFilterUI("search_keyword", "current_query", n.value, !0, "") } let c = a.querySelector(`.selected-filter-item[data-taxonomy="${e}"][data-term-id="${s}"]`); if (c) { let n = c.querySelector(".filter-name"); n && (n.textContent = i); return } let o = document.createElement("li"); o.className = "selected-filter-item", o.setAttribute("data-taxonomy", e), o.setAttribute("data-term-id", s), o.setAttribute("data-taxonomy-label", r); let u = document.createElement("span"); u.className = "filter-name", u.textContent = i; let l = document.createElement("button"); l.type = "button", l.className = "remove-filter", l.setAttribute("aria-label", "Remove filter"), l.textContent = "\xD7", o.appendChild(u), o.appendChild(l), a.appendChild(o), this.$selectedFilters.style.display = "" } else { if (!a) return; let c = a.querySelector(`.selected-filter-item[data-taxonomy="${e}"][data-term-id="${s}"]`); c && c.remove(), a.children.length === 0 && (this.$selectedFilters.style.display = "none") } } syncSelectedFiltersFromHTML(e) { if (!this.$selectedFilters) { let l = document.createElement("div"); l.innerHTML = e; let n = l.firstElementChild; if (n) { let d = document.querySelector(".jankx-search-selected-filters"); d && (d.outerHTML = n.outerHTML), this.$selectedFilters = document.querySelector(".jankx-search-selected-filters") } return } let t = new DOMParser().parseFromString(e, "text/html").body.firstElementChild; if (!t) { this.clearAllSelectedFiltersUI(); return } let r = t.style.display !== "none", a = Array.from(t.querySelectorAll(".selected-filter-item")), o = this.$selectedFilters.querySelector(".selected-filters-list"); if (a.length > 0 && !o) { let l = document.createElement("ul"); l.className = "selected-filters-list"; let n = this.$selectedFilters.querySelector(".clear-all-wrapper"); n ? this.$selectedFilters.insertBefore(l, n) : this.$selectedFilters.prepend(l), o = l } let u = new Set; a.forEach(l => { let n = l.getAttribute("data-taxonomy"), d = l.getAttribute("data-term-id"), h = `${n}|${d}`; u.add(h); let p = (l.querySelector(".filter-name")?.textContent || "").trim(), m = l.getAttribute("data-taxonomy-label") || "", v = p; m && p.startsWith(`${m}: `) && (v = p.replace(`${m}: `, "")), this.updateSelectedFilterUI(n, d, v, !0, m) }), o && (Array.from(o.querySelectorAll(".selected-filter-item")).forEach(n => { let d = n.getAttribute("data-taxonomy") || "", h = n.getAttribute("data-term-id") || "", y = `${d}|${h}`; u.has(y) || d === "search_keyword" || n.remove() }), o.children.length === 0 ? this.$selectedFilters.style.display = "none" : this.$selectedFilters.style.display = "") } decodeHTMLEntities(e) { let s = document.createElement("textarea"); return s.innerHTML = e, s.value } updateFeaturedItemsVisibility() { if (!this.$featuredItems) return; let e = !!this.state.q, s = Object.values(this.state.filters).some(i => i.length > 0); e || s ? this.$featuredItems.style.display = "none" : this.$featuredItems.style.display = "" }
        }; document.addEventListener("DOMContentLoaded", () => { new f })
    }); $();
})();
